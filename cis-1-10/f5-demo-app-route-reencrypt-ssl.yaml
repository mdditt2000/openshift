  apiVersion: v1
  kind: Route
  metadata:
    labels:
      name: f5-demo-app-route-reencrypt-ssl
    name: f5-demo-app-route-reencrypt-ssl
    namespace: f5demo
    annotations:
      # annotations
      # default clientssl profile
      # health monitoring
      virtual-server.f5.com/clientssl: /Common/clientssl
      virtual-server.f5.com/clientssl: /Common/serverssl
      virtual-server.f5.com/health: |
        [
          {
            "path": "mysite.f5demo.com/",
            "send": "HTTP GET /",
            "interval": 5,
            "timeout": 10
          }
        ]
  spec:
    host: mysite.f5demo.com
    path: "/"
    port:
      targetPort: 8080
    tls:
      termination: reencrypt
    to:
      kind: Service
      name: f5-demo-app-route-reencrypt-ssl